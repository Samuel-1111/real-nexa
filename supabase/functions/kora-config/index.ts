const CORS={"Access-Control-Allow-Origin":"*","Access-Control-Allow-Headers":"authorization, x-client-info, apikey, content-type","Access-Control-Allow-Methods":"GET, OPTIONS"};
Deno.serve(async req=>{if(req.method==="OPTIONS")return new Response("ok",{headers:CORS});const key=Deno.env.get("KORA_PUBLIC_KEY")||"";if(!key)return new Response(JSON.stringify({error:"KORA_PUBLIC_KEY is not configured in Supabase Secrets."}),{status:503,headers:{"Content-Type":"application/json",...CORS}});return new Response(JSON.stringify({public_key:key}),{headers:{"Content-Type":"application/json",...CORS}})});
